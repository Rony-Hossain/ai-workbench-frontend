# electron-bridge

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build electron-bridge` to build the library.

## Running unit tests

Run `nx test electron-bridge` to execute the unit tests via [Vitest](https://vitest.dev/).
