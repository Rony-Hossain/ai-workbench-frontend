# rag-fs-ingest

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build rag-fs-ingest` to build the library.

## Running unit tests

Run `nx test rag-fs-ingest` to execute the unit tests via [Vitest](https://vitest.dev/).
